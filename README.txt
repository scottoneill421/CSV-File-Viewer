This web application is a simple csv viewer for an old work project.

Any csv file added to the "csv-files" directory should have it's own link
generated on the index page, and that link calls a function that formats 
and displays the csv file as a data table.
